TFCipher is a brute forcer I made based on the TwoFishLib in the tar archive in the folder.

To install archive:

tar -xf twofish-0.3.0.tar.gz
cd twofish-0.3.0
sudo python setup.py build
sudo python setup.py install
